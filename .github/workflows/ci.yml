name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          # pnpm 11 (from packageManager) requires Node >= 22.13 — it uses
          # node:sqlite, which does not exist on Node 20.
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - name: Validate school data
        run: pnpm test tests/data-integrity.test.ts
      - name: Unit tests
        run: pnpm test
